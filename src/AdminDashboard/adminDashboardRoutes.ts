import express from "express"
import { apiVersionOneRoute } from ".."

export default function AdminDashboardRoutes(app: express.Application, jsonParser:any){
  // app.get(
  //   `${apiVersionOneRoute}getUsersAccount`
  // )
}