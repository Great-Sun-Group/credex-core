import * as s from "../../utils/inputSanitizer";
import * as v from "../../utils/validators";

export const clearDevDBsSchema = {
  // No parameters required for clearing dev DBs
  // Empty schema since this is a development-only operation
};

export const forceDCOSchema = {
  // No parameters required for forcing DCO
  // Empty schema since this is a development-only operation
};
