{
  "ignore": [".git", "node_modules", "dist", "build"],
  "watch": ["src", "config"],
  "exec": "ts-node -r dotenv/config src/index.ts",
  "ext": "ts,json",
  "env": {
    "NODE_ENV": "development"
  }
}
